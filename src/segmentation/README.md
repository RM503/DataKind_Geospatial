# **GeoTIFF segmentation pipeline**

The segmentation pipeline (`src/segmentation/`) contains scripts for performing naive field delineation on raster tiles. It leverages AWS SageMaker's `ScriptProcessor` module for performing segmentation jobs from a local machine. 

## **Directory Structure**

The directory structure for segmentations jobs is defined as follows —
